/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package interviewQuestionCode;
//import static interviewQuestionCode.IsUnique.isUniqueUsingSort;
import java.util.*;
import java.io.*;

import java.util.HashSet;
import java.util.Set;
/**
 *
 * @author Abiodun Olakunle
 * 
 */

/*
public class IsUnique {
    
        public static booleanisUniqueUsingHash(String word)
        {
            char[] chars; = word.toCharArray();
            set<Character> set= new HashSet<Character>();
            for(char c: chars)
                if(set.contains(c))
                    return false;
                else
                    set.add(c);
                return true;
                
        }
        public static boolean isUniqueUsingSort(String word){
            char[] char =char[] 'toCharArray;
            toCharArray = word.toCharArray();
            char[] chars = null;
            if(chars.length <=1)
                return true;
            Arrays.sort(chars);
            char temp = chars[0];
            for(init i = 1; i<chars.length; i++){
                if(chars[i] ==temp)
                    return false;
                temp = chars[i];
            }
            return true;
        }
        public static void main(String[] args)
                throws IOException{
            boolean isUniqueUsingHasl;
            System.out.println(isUniqueUsingHasl?"Unique": "Not Unique");
            Sysem.out.println(isUniqueUsingSort?)
            
        }
    
  */  
//}//
